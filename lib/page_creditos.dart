import 'package:flutter/material.dart';

class TelaCreditos extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tela de creditos'),
      ),
      body: Center(
        child: Text('Bem-vindo à Tela de creditos'),
      ),
    );
  }
}
